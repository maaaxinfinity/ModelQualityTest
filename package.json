{
  "name": "model-quality-test",
  "version": "0.2.0",
  "private": true,
  "description": "Multi-provider model quality and channel probe with Vercel APIs, PostgreSQL logs, TOTP auth, and cost estimates.",
  "scripts": {
    "postinstall": "node scripts/install-tectonic.js",
    "check": "node --check app.js && node --check questions.js && find api -name '*.js' -print0 | xargs -0 -n1 node --check",
    "preflight": "node tests/preflight.js",
    "test:logic": "node tests/logic.test.js",
    "test:report": "node tests/report.test.js",
    "test:db": "node tests/db-smoke.js && node tests/auth-smoke.js && node tests/run-test-smoke.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1",
    "pg": "^8.13.1",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "vercel": "^54.18.0"
  }
}
